import React from 'react';
import { AdContainer } from '../AdminCommonElements';

const FeedbackManage = ({ meData }) => {
  console.log('me', meData);

  return (
    <AdContainer>
      <h1>Feedback Lists</h1>
    </AdContainer>
  );
};

export default FeedbackManage;
